useEffect(() => {
  loadScanner(false);
  setPage(1);
  // eslint-disable-next-line react-hooks/exhaustive-deps
}, [scannerType]);


<button
  onClick={() => loadScanner(true)}
  disabled={refreshing}
  className="rounded-full border border-white/10 bg-white/[0.03] px-5 py-2.5 text-sm font-medium text-slate-300 transition hover:bg-white/[0.06] disabled:opacity-50"
>
  {refreshing ? "Refreshing..." : "Refresh"}
</button>
