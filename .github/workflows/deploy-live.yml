name: Deploy Aurora Live

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Start SSH agent
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.LIVE_SSH_KEY }}

      - name: Add server to known_hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -p "${{ secrets.LIVE_PORT }}" "${{ secrets.LIVE_HOST }}" >> ~/.ssh/known_hosts

      - name: Deploy live on server
        run: |
          ssh -p "${{ secrets.LIVE_PORT }}" "${{ secrets.LIVE_USER }}@${{ secrets.LIVE_HOST }}" 'bash /home/paul/deploy.sh'
